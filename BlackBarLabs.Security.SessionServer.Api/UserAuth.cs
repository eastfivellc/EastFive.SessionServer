using BlackBarLabs.Security.AuthorizationServer.API.Controllers;

namespace BlackBarLabs.Security.AuthorizationServer.API
{
    public class UserAuth : BaseController
    {
            //var session = DataContext.Sessions.FindById(item.SessionId);
            //if (session == null) return NotFound();
            //if (item.Email == "" || item.Password == "") return BadRequest("Must specify email and password credentials.");
            //var email = item.Email;
            //var password = item.Password;
            //if (string.IsNullOrEmpty(email)) return BadRequest("Email cannot be null or empty.");
            //if (string.IsNullOrEmpty(password)) return BadRequest("Password cannot be null or empty.");
            //var authenticated = await session.AuthenticateAsync(email, password);
            //if (!authenticated) return Unauthorized();
            //var token = string.Empty;
            //await session.ExtrudeAuthenticationInformationAsync((s, token1) => token = token1);
            //return Ok(token);
    }
}